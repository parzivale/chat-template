{
  "name": "clearip-text-editor",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx parcel build src/index.html --no-cache --no-scope-hoist",
    "postbuild": "node inline.mjs",
    "watch": "npx parcel src/index.html --no-source-maps --no-cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "inline-source": "^8.0.3",
    "node-html-parser": "^6.1.13",
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "@types/node": "^22.5.1",
    "talkjs": "^0.26.0"
  }
}
